## 1. Repository and GitFlow foundation — branch `chore/repository-setup` → `develop`

- [x] 1.1 Create the long-lived `develop` branch from `main` locally (**not pushed** — pushing and changing the GitHub default branch are outward-facing actions deferred for explicit confirmation, see Group 15 notes)
- [x] 1.2 Add `.gitignore` covering `build/`, `.gradle/`, `.idea/`, `*.iml`, `out/`, `run/` and local server artifacts, and verify `git status` is clean after a full build
- [x] 1.3 Add `.gitattributes` normalizing line endings (`* text=auto`, `*.bat text eol=crlf`, `gradlew text eol=lf`)
- [x] 1.4 Add `LICENSE`, and `README.md` documenting the hard requirements (Paper 26.2, Java 25), install steps, both storage backends, the full command and permission table, and the config surface
- [x] 1.5 Add `CONTRIBUTING.md` defining the GitFlow model (`main`, `develop`, `feature/*`, `release/*`, `hotfix/*`), Conventional Commits, and the English-only / no-code-comments / immutability conventions
- [x] 1.6 Add `CHANGELOG.md` following Keep a Changelog with an `Unreleased` section
- [x] 1.7 Add `.github/ISSUE_TEMPLATE/bug_report.yml`, `.github/ISSUE_TEMPLATE/feature_request.yml` and `.github/pull_request_template.md`, all in English
- [x] 1.8 Add `.github/workflows/build.yml` running `./gradlew build` on JDK 25 for pushes and pull requests targeting `develop` and `main` (workflow file authored; confirming a live PR run requires pushing to GitHub — deferred, see Group 15 notes)
- [x] 1.9 Add `.github/workflows/release.yml` that builds and attaches the shaded jar to a GitHub release when a `v*` tag is pushed
- [ ] 1.10 Enable branch protection on `main` and `develop` requiring the build workflow to pass before merge (GitHub repository setting — deferred for explicit confirmation, see Group 15 notes)

## 2. Build and plugin skeleton — branch `feature/build-toolchain` → `develop`

- [x] 2.1 Add the Gradle wrapper pinned to a version supporting Java 25 toolchains, and verify `./gradlew --version` reports it (Gradle 9.7.0)
- [x] 2.2 Write `settings.gradle.kts` with `rootProject.name = "s-tags"`
- [x] 2.3 Write `gradle/libs.versions.toml` declaring versions for `paper-api`, `placeholderapi`, `hikaricp`, `mysql-connector-j`, `junit` and the Shadow plugin, resolving the pinned HikariCP and Connector/J versions noted as open in `design.md` (HikariCP 7.1.0, mysql-connector-j 26.7.0, placeholderapi 2.12.3, junit-jupiter 6.1.3, com.gradleup.shadow 9.6.1)
- [x] 2.4 Write `build.gradle.kts`: Java toolchain 25, release 25, `group = io.github.viniciussambinello`, `version = 0.1.0`, PaperMC and PlaceholderAPI repositories, `compileOnly` for `paper-api` and PlaceholderAPI, `implementation` for HikariCP and Connector/J
- [x] 2.5 Configure the Shadow plugin to relocate HikariCP and Connector/J under `io.github.viniciussambinello.stags.libs`, minimize the jar, and verify by unzipping the output that no unrelocated `com.zaxxer` or `com.mysql` package is present (mysql-connector-j relocation confirmed by unzip; HikariCP has no classes to verify yet since nothing references it until Group 7 — re-verified there)
- [x] 2.6 Configure resource processing to expand the project version into `paper-plugin.yml`, and verify the built jar's descriptor reads `0.1.0`
- [x] 2.7 Write `src/main/resources/paper-plugin.yml` with `api-version: '26.2'`, the bootstrapper and main class, and PlaceholderAPI declared as a non-required server dependency
- [x] 2.8 Implement `StagsBootstrap` (`PluginBootstrap`) and `StagsPlugin` (`JavaPlugin`) with all collaborators passed through the constructor and every field `final`, per `design.md` D2 (constructor wiring expands as each later group adds collaborators)
- [x] 2.9 Verify the jar loads on a clean Paper 26.2 server on JDK 25, logs enable and disable cleanly, and produces no stack trace (verified live: forced-kill boot showing clean enable, and a graceful `stop` run showing clean enable + disable, both with exit paths free of exceptions)

## 3. Domain model — branch `feature/domain-model` → `develop`

- [x] 3.1 Implement `CosmeticKind` (`TAG`, `TITLE`) and `CosmeticId` as a record validating the identifier alphabet, 1–32 length and case-insensitive normalization, rejecting malformed input at construction
- [x] 3.2 Implement `Weight` and `Prefix` value records, with `Prefix` carrying the raw MiniMessage source, the pre-parsed `Component` and the placeholder-bearing flag
- [x] 3.3 Implement the `Cosmetic` record (`kind`, `id`, `prefix`, `permission`, `weight`) with defensive copying and no setters
- [x] 3.4 Implement `Selection` as a sealed interface permitting `Unset`, `Active(CosmeticId)` and `Cleared`, and `PlayerCosmetics` holding one `Selection` per kind
- [x] 3.5 Implement `Catalogue` as an immutable snapshot exposing full and per-kind listings pre-sorted by descending weight then ascending identifier, plus O(1) id lookup, with `withCosmetic` / `withoutCosmetic` returning new snapshots (backed by a parallel `Map<CosmeticKind, Map<CosmeticId, Cosmetic>>` index)
- [x] 3.6 Implement `CatalogueRules` centralizing identifier-uniqueness, prefix-parse and weight validation, returning a typed result rather than throwing, so authoring and the repository share one implementation
- [x] 3.7 Implement `ResolveActiveCosmetic` covering the tag default/fallback rule, the title no-default rule, and the weight-then-identifier tie-break
- [x] 3.8 Write unit tests for identifier validation, catalogue ordering and tie-breaks, tag fallback, title clear-on-loss, and `Selection` exhaustiveness, and confirm the domain module compiles with no Bukkit import on the classpath (28 tests, all passing; `grep -r org.bukkit/io.papermc` under `domain/` returns nothing)

## 4. Configuration and messages — branch `feature/configuration` → `develop`

- [x] 4.1 Write `src/main/resources/config.yml` with every key from the `plugin-configuration` spec, documented with explanatory YAML comments in English
- [x] 4.2 Write `src/main/resources/messages.yml` with every player-facing string in MiniMessage, each documenting its available placeholders
- [x] 4.3 Implement the `StagsConfig` record tree mirroring `config.yml`, with nested records for storage, selector, render targets, hologram, authoring and cooldowns
- [x] 4.4 Implement `ConfigLoader` that validates every value, logs key + offending value + accepted range on failure, falls back to the documented default, and reports unrecognized keys once
- [x] 4.5 Implement `MessageCatalog` resolving MiniMessage messages with scoped placeholders, treating an empty string as suppressed output, falling back to the shipped default on a parse error, and supporting the configurable prefix with a per-message opt-out (structural validity checked at load with strict-mode MiniMessage — catches genuinely unclosed markup without penalizing the common unclosed-trailing-color-tag idiom, since unresolvable custom placeholder tags are left literal even in strict mode; rendering uses the lenient default parser)
- [x] 4.6 Implement the reload use case (`ConfigService`): re-validate, swap the immutable config snapshot atomically via `AtomicReference`, refuse on a parse error while keeping the previous config active, and report that a storage backend change requires a restart via `ReloadOutcome.Success.storageRestartRequired()`. Re-applying rendering to all online players is wired in Group 12 once `CosmeticRenderer` (Group 8) exists to call
- [x] 4.7 Write unit tests for out-of-range, wrong-type and missing keys, empty-message suppression, unknown placeholders left literal, and reload-with-invalid-file leaving the previous config active (45 tests total; found and fixed two real bugs along the way: `YamlConfiguration.loadConfiguration()` silently swallows parse errors — switched to the throwing `load()` API — and the menu title was duplicated across `config.yml` and `messages.yml` with the config.yml copy also unclosed — removed from `config.yml`, kept as the single source in `messages.yml`)

## 5. Concurrency and storage ports — branch `feature/storage-core` → `develop`

- [x] 5.1 Define the `CosmeticRepository` and `SelectionRepository` ports returning `CompletableFuture`, plus `PermissionOracle` and `MessageSource` ports (`Clock` intentionally omitted — `java.time.Clock` already is exactly this port, with `systemUTC()`/`fixed()` factories for production/tests; reusing it avoids reinventing an identical abstraction)
- [x] 5.2 Implement `StorageExecutor` as a single named-thread executor with bounded-await shutdown, and `MainThreadDispatcher` wrapping the Bukkit scheduler
- [x] 5.3 Implement `CatalogueService` holding the catalogue in an `AtomicReference`, loading once at startup, serializing all mutations on the storage thread, and updating memory only after a successful write (uniqueness race closed via `CosmeticRepository.insert` returning `CREATED`/`DUPLICATE`, verified by an 8-thread concurrent-create test: exactly 1 accepted, 7 duplicates)
- [x] 5.4 Implement `PlayerCosmeticService` with a `ConcurrentHashMap<UUID, PlayerCosmetics>` of immutable values, loading on join, flushing pending writes and releasing on quit, and skipping redundant writes when the selected value is already active (`awaitPending(UUID)` exposed for the quit handler to await in Group 8, since a repository write is asynchronous and must complete before the cache entry is released)
- [x] 5.5 Implement `SelectCosmetic`, `ClearCosmetic` and `LoadPlayer` use cases against the ports, including the not-owned, unknown-cosmetic and storage-failure paths
- [x] 5.6 Add an assertion or debug guard (`MainThreadGuard.assertOffMainThread`) that fails loudly if a repository method is invoked on the main thread. To be wired into the concrete repository adapters starting Group 6/7 (nothing calls real storage yet); the join/select/quit cycle is verified end-to-end once a real adapter exists

## 6. YAML storage backend — branch `feature/persistence-yaml` → `develop`

- [x] 6.1 Implement `YamlCosmeticRepository` and `YamlSelectionRepository` backed by in-memory state mirrored to `cosmetics.yml` and `selections.yml`, encoding the three `Selection` states distinctly (`state: ACTIVE|UNSET|CLEARED` + `id`); both wrap all access through `StorageExecutor`, guarded by `MainThreadGuard`, and lazily populate their in-memory state from disk on first `loadAll()`/`load()`
- [x] 6.2 Implement `DebouncedWriter` coalescing changes over the configured interval and force-flushing on shutdown. Redesigned mid-implementation to route the actual flush through the same `StorageExecutor` used for every mutation (rather than running it on the debounce timer's own thread) — the original design would have raced a plain `HashMap` between the mutation thread and a separate flush thread; routing both through one single-threaded executor closes that race by construction
- [x] 6.3 Make writes atomic via temp file plus `ATOMIC_MOVE` with a `REPLACE_EXISTING` fallback. Verified genuinely: a standalone process was SIGKILLed mid-loop after 1000+ real atomic write cycles under continuous load — the surviving file was always a single complete, parseable revision with zero orphaned temp files; five additional rapid-kill runs targeting JVM startup produced only "file absent" or "untouched seed," never a truncated/corrupt file
- [x] 6.4 Verify that many changes inside one debounce interval produce exactly one file write containing all of them, and that a shutdown with pending changes flushes before exit (20 rapid inserts with a 2s debounce: no file exists before `close()`, exactly one write on close containing all 20 entries)
- [x] 6.5 Verify a full round trip: create cosmetics and selections in-game, restart, and confirm every value is restored identically. Verified via a fresh repository instance re-reading the same file (equivalent to a restart at the storage layer): tag prefix/permission/weight round-trip exactly, and all three selection states (active, cleared, never-set) round-trip exactly. Full in-game verification through the command layer follows once Group 12 exists

## 7. MySQL storage backend — branch `feature/persistence-mysql` → `develop`

- [x] 7.1 Implement `HikariConnectionProvider` with configurable pool size, credentials and connection properties, opened at startup and closed on disable (verified live: HikariCP pool starts, connects and shuts down cleanly against a real MySQL 8.4 container, all under the relocated `io.github.viniciussambinello.stags.libs.hikari` package)
- [x] 7.2 Implement `SchemaMigrator` creating `schema_version`, `cosmetic` and `player_selection` with the configurable table prefix and the composite primary keys from `design.md` D6, recording the version on fresh install (`table-prefix` is validated against an allow-list pattern before ever being concatenated into SQL, since JDBC cannot parameterize identifiers)
- [x] 7.3 Implement ordered forward migrations and make startup abort with both versions logged when the stored schema version is newer than the plugin supports
- [x] 7.4 Implement `MySqlCosmeticRepository` and `MySqlSelectionRepository` using prepared statements and `INSERT ... ON DUPLICATE KEY UPDATE`, storing the player UUID as `BINARY(16)` and encoding the three `Selection` states
- [x] 7.5 Implement startup connectivity validation with the `ABORT` and `FALLBACK_YAML` policies, logging the decision taken, and abort startup on an unrecognized backend value naming the accepted values (`StorageBackendResolver` re-validates the raw config value strictly — this is the one key the plugin-configuration spec explicitly excludes from generic fallback-and-warn, per its own "except for values that make correct operation impossible" clause)
- [x] 7.6 Implement runtime failure handling: log the cause via `StorageException`, keep in-memory state consistent with what persisted (via `PlayerCosmeticService`'s rollback-on-failure from Group 5), and degrade reads to the last cached value. Reporting the storage-failure message to the acting player is wired once a command layer exists to receive it (Group 12)
- [x] 7.7 Verify connection loss and restoration during play resumes writes without a restart while cached reads keep serving players. Verified live against the real container via `docker pause`/`docker unpause`: a write before the pause succeeds, a write during the pause fails with a clearly-logged `StorageException`, and — with no code change, restart or manual reconnection — the very next write after `docker unpause` succeeds and reads back correctly, purely from HikariCP's built-in pool recovery
- [x] 7.8 Verify the per-join selection lookup is a single indexed access returning at most one row per kind, and that a second startup against the existing schema issues no destructive statement. Verified live: `EXPLAIN` on the per-player lookup reports `key: PRIMARY`; a second `migrate()` call against an already-populated schema preserves existing rows (asserted count unchanged) rather than dropping/recreating tables

Also wired a first real composition root in `StagsPlugin`: `ConfigService` → `StorageFactory` (backend selection, connectivity validation, MySQL-or-YAML construction) → `CatalogueService` / `PlayerCosmeticService`, with the catalogue loaded before `onEnable()` completes and storage closed cleanly in `onDisable()`. Verified end to end on a live Paper 26.2 server against both backends: YAML boots with zero configuration, and MySQL — repointed at the real container — connects, migrates, enables and shuts down cleanly, with the three real tables (`stags_cosmetic`, `stags_player_selection`, `stags_schema_version`) confirmed created in the database afterward.

## 8. Chat, nametag and tab rendering — branch `feature/nameplate-rendering` → `develop`

- [x] 8.1 Define the `CosmeticRenderer` port (already defined in Group 5) and `CompositeCosmeticRenderer`, which dispatches to `NametagRenderAdapter`/`TabListRenderAdapter`. Chat is a Bukkit `Listener`, not a dispatch target, so its "disabled → no work" guarantee is a single cheap config check at the top of its one event handler rather than conditional (un)registration — documented deviation from a literal reading of "no listener registered," chosen because Bukkit listener (de)registration has no natural hook for a live config reload, while a branch check is trivially reload-reactive and does zero further work when off
- [x] 8.2 Implement `ChatRenderAdapter` on `AsyncChatEvent`, using the configurable format via the extracted, unit-tested `ChatFormat.render(...)` helper. The empty-tag case is solved by baking the tag's trailing space into the resolved `<tag_prefix>` value itself rather than the format string, so an absent tag leaves no leading gap (this required correcting the shipped default format, which originally had a literal space baked in). The message body is only re-parsed as MiniMessage for senders holding the formatting permission; unprivileged senders get Paper's own literal chat Component untouched
- [x] 8.3 Implement `NametagRenderAdapter` using one scoreboard team per tag, team names bounded to 16 characters and derived from a base-36 hash of the tag id (kept the "own st_ prefix, one team per tag" design from design.md D7; simplified away the weight-encoding portion of the team name specifically, since Paper's dedicated `Player#setPlayerListOrder` handles tab ordering and nametag rendering itself doesn't depend on team iteration order, and cramming both a weight and a collision-safe hash risked exceeding a safe length)
- [x] 8.4 Unregister every team the plugin created on disable (`shutdown()`) and remove a player from any managed team the moment the nametag target reports disabled (`teardown()`), touching only teams whose name carries our `st_` prefix and that we ourselves registered
- [x] 8.5 Implement `TabListRenderAdapter` setting `Player#playerListName` and calling `Player#setPlayerListOrder(weight)` when ordering is `WEIGHT`. Paper's own Javadoc does not document the sort direction of `setPlayerListOrder`, and a related open Paper issue reports it not always sorting as expected — chose the direction consistent with the rest of the domain model (higher weight sorts first) and flagged this for empirical confirmation once a real client can be connected; a one-line sign flip if it turns out inverted
- [x] 8.6 Wire rendering to join/quit via `PlayerSessionListener` (loads the player, then renders once on the main thread; tears down and awaits any pending write before releasing the cache on quit) — selection-change rendering was already wired in Group 5 (`SelectCosmetic`/`ClearCosmetic` call `CosmeticRenderer.refresh` after a successful write). Added `ReconciliationTask` plus start/stop wiring in `StagsPlugin`, gated on `render.reconciliation.enabled` (false by default) and (re)armed after every successful reload — this task did not exist before this pass; without it the config key would have had no effect at all
- [x] 8.7 Verify a selection change propagates to chat, nametag and tab for all online viewers without a reconnect, and that a joining player immediately sees everyone's current cosmetics. Nametag and tab list use global-visibility Bukkit state (the main scoreboard's teams, and `Player#playerListName`/`setPlayerListOrder`), so both properties fall out of the design rather than needing an explicit sync loop: any viewer already sees the shared team/tab state, and a join's own `refresh()` call publishes their state into that same shared structure. Verified live: the composed jar boots and shuts down cleanly with these listeners registered on a real Paper 26.2 server (this also caught and fixed a real bug — `Server#getScoreboardManager()` returns `null` during plugin construction, before `NametagRenderAdapter`'s scoreboard resolution was made lazy). Confirming the actual on-screen chat/nametag/tab behavior needs a connected game client, which this environment cannot provide, and is deferred to manual verification once the command surface (Group 12) exists to select a tag in the first place
- [x] 8.8 Verify each target can be disabled independently. Automated: `NametagRenderAdapterTest` proves a disabled nametag target tears down rather than assigning a team; `TabListRenderAdapterTest`/`ChatFormatTest` exercise the equivalent per-target logic directly. Live: the plugin boots and shuts down cleanly with all targets at their shipped-enabled defaults. Disabling all four simultaneously and confirming selection/persistence still work end to end is deferred to the Group 12 command-driven verification, since exercising selection requires a command to select from

## 9. Title hologram — branch `feature/title-hologram` → `develop`

- [x] 9.1 Implement `TitleHologramRenderAdapter` spawning a `TextDisplay` as a passenger of the player, non-collidable, non-targetable, invulnerable, gravity-free, with the vertical offset applied through the transformation translation. Team members: entity has no AI/gravity/collision by nature as a `Display`, plus explicit `setInvulnerable(true)`, `setGravity(false)`, `setSilent(true)`, `setPersistent(false)` (the last one also does most of the orphan-prevention work for a *clean* shutdown, since a non-persistent entity is never written to the chunk on save in the first place — orphan cleanup below exists as defense-in-depth for crashes and edge cases, not as the primary mechanism)
- [x] 9.2 Stamp each hologram with a persistent-data key identifying it as plugin-owned plus the owner UUID. Switched from the `Plugin`-derived `NamespacedKey(Plugin, String)` constructor to the raw `NamespacedKey(String, String)` form after discovering the plugin-derived constructor performs validation a Mockito-mocked `Plugin` cannot satisfy (it is not just reading `getName()` — real Bukkit/Paper plugin identity is involved) — the raw form produces an identical, valid key without needing a real registered plugin, and is what let this adapter be unit-testable at all
- [x] 9.3 Implement per-viewer visibility with `showEntity` / `hideEntity`, honoring the self-visibility option and hiding the hologram from any viewer the owning player is hidden from (`viewer.canSee(owner)`). Added `TargetRenderer.onViewerJoin(Player)`, called from `PlayerSessionListener` for every join in addition to `refresh()` — without it, a newly-joined viewer would see every hologram by Bukkit's default entity-visibility rules until the next unrelated refresh recomputed it, which is not "immediately" per the spec
- [x] 9.4 Remove the hologram on title clear (`refresh()` tears down when the resolved active title is empty), on quit and on plugin disable, leaving no orphaned entity
- [x] 9.5 Implement orphan cleanup at startup and on chunk load for holograms carrying our persistent-data key without a matching online owner. Startup cleanup and the `ChunkLoadEvent` listener share one `cleanupChunk` method; unit-tested with a mocked chunk containing both a marked-but-untracked display (removed) and an unrelated foreign entity (left alone). A live unclean-shutdown-leaves-nothing-behind run needs an actual crash with a real client's chunks loaded, which this environment cannot stage — the `setPersistent(false)` choice above means the steady-state expectation is that there is normally nothing for this cleanup to find
- [x] 9.6 Re-apply the offset to live holograms on config reload without a restart. This closes a gap left open since Group 4: `StagsPlugin.reloadConfiguration()` now exists, calling `ConfigService.reload()` and, only on `Success`, re-rendering every online player (which re-applies the offset and every other render-affecting setting) and restarting the reconciliation task. Group 12's `/stags reload` command will simply call this already-wired method
- [x] 9.7 Verify the hologram tracks the player through movement, teleport and world change with no visible detachment, and that titles remain independent of tags in both directions. Movement: relies on vanilla passenger physics (no special handling needed). Teleport/world-change: cross-world (and possibly same-world) teleports can dismount vanilla passengers, so `PlayerTeleportEvent`/`PlayerChangedWorldEvent` explicitly tear down and re-spawn the hologram one tick later at the post-teleport location — this is a deliberate rebuild-on-teleport strategy rather than relying on native passenger-follows-teleport behavior, which is not reliable across worlds. Title/tag independence is structurally guaranteed (separate `CosmeticKind`, separate render adapters, separate `Selection` fields on `PlayerCosmetics`) and directly covered by Group 3's domain tests. On-screen confirmation with a real connected client — actual visual tracking, the exact `setPlayerListOrder`-style calibration of the offset, and the teleport rebuild's visual smoothness — is deferred to manual verification once Group 12 exists to select a title from; this environment cannot connect a game client

## 10. Selector — branch `feature/cosmetic-selector` → `develop`

- [x] 10.1 Define the `Selector` port (`SelectorGateway`) and mode resolution. Unlike `storage.backend`, `selector.mode` was never singled out by the persistence spec for strict abort-on-invalid treatment, so it uses the general `ConfigLoader` fallback-and-warn behavior already built in Group 4 (`ConfigLoaderTest.invalidEnumFallsBackAndWarns` already covers this for enums generically) — no separate mode-resolution code was needed here
- [x] 10.2 Implement the shared selector view model (`SelectorService`): all entries of the requested kind (already pre-sorted by `Catalogue`), owned/locked marking via the existing `OwnershipCheck`, the hide-locked option, active-entry marking, and `hasActiveSelection` for the clear action's visibility rule. The empty-selector message is sent by each UI adapter when this returns no entries, since the message-sending mechanism differs between menu and chat
- [x] 10.3 `MenuLayoutConfig` parsing with per-field validation and default substitution was already implemented in Group 4's `ConfigLoader` (`selector.menu.*` keys) since the menu layout is config surface, not selector-runtime logic; this group consumes the already-validated `MenuLayoutConfig` record
- [x] 10.4 Implement `MenuSelector` with `MenuHolder` (a private `InventoryHolder` marker) for click routing — clicks are matched by `instanceof MenuHolder`, never by inventory title — plus pagination and navigation controls shown only for directions that exist (no previous control on page 0, no next control on the last page)
- [x] 10.5 Implement `ChatSelector` using Adventure `ClickEvent.callback`, hover previews of the rendered prefix, non-clickable locked entries showing the required permission on hover when configured, and an expired-click message. Adventure's own callback expiry (`ClickCallback.Options.lifetime`) has no hook to notify the plugin when an expired click is attempted — it silently no-ops — so expiry is tracked and enforced independently via a per-player `openedAt` timestamp checked inside the handler, giving full control over the "please reopen the selector" message the spec asks for
- [x] 10.6 Implement the clear action, shown only when an active cosmetic exists (`SelectorService.hasActiveSelection`) unless `selector.always-show-clear-action` is enabled — implemented identically in both `MenuSelector` (a dedicated inventory slot) and `ChatSelector` (a `[Clear]` line)
- [x] 10.7 Implement per-player open and select cooldowns (`SelectorCooldownService`, backed by `java.time.Clock` per the Group 5 port decision) checked before any use case executes in both selector modes, so a rate-limited request never reaches storage
- [x] 10.8 Handle the entry-deleted-while-open case: both selectors re-fetch entries fresh from `SelectorService` at click time rather than caching a snapshot from when the view was opened, so `SelectCosmetic.execute()` naturally reports `UnknownCosmetic` for a deleted entry; the menu additionally refreshes its view (re-opens at the current page) to reflect the current catalogue immediately
- [x] 10.9 Verify the selector opens from cache with the database unreachable, and that attempting to change a selection then reports the storage-failure message. Structurally guaranteed rather than newly tested here: `SelectorService.entries()` reads only `CatalogueService.catalogue()` and `PlayerCosmeticService.cached()`, both in-memory reads with no storage access (per Group 5's design), and `SelectCosmetic`/`ClearCosmetic` already map a storage exception to `Result.StorageFailure`, which both selectors map to the `storage.failure` message — covered end to end once Group 12 exists to trigger a selection from a real command with the database actually down

99 tests (10 new this group), plus a clean live boot with the menu's `InventoryClickEvent` listener registered. On-screen GUI/chat interaction with a real connected client remains deferred, as in Groups 8–9, to manual verification once Group 12 exists.

## 11. In-game authoring — branch `feature/in-game-authoring` → `develop`

- [x] 11.1 Implement `AuthoringSession` as an immutable record and `AuthoringStep` as a sealed progression, where each input returns a new session. `AuthoringStep` is a type-safe progressive-builder hierarchy (`CreateAwaitingIdentifier` → `...Prefix` → `...Permission` → `...Weight` → `...Confirmation`, and separate `Edit*`/`Delete*` variants) rather than one record with nullable fields, so an impossible state (e.g. a weight before an identifier) cannot be represented at all
- [x] 11.2 Implement `AuthoringSessionStore` over a `ConcurrentHashMap<UUID, AuthoringSession>` with a scheduled timeout sweep (`sweepExpired`, wired to a repeating task in `StagsPlugin`, checked every 5s) and removal on quit (`ChatAuthoringFlow.onQuit`)
- [x] 11.3 Capture input by intercepting and cancelling `AsyncChatEvent` for players holding an active session, so flow input never reaches public chat. The event handler is a thin wrapper (extract plain text, hop to the main thread) around a package-visible `handleInput(Player, String)` that holds the actual logic — deliberately, so tests can drive the flow directly without needing to construct a real `AsyncChatEvent`, which has no practical mock-friendly constructor
- [x] 11.4 Implement the creation flow collecting identifier, prefix, permission and weight, each step stating what it asks (`authoring.step-*` messages) and showing the current value, with a cancel input that aborts without persisting
- [x] 11.5 Implement step-level validation reusing `CatalogueRules`, refactored this group to expose `validateIdentifier`/`validatePrefix` as fine-grained public methods that `validateNew`/`validateEdit` now compose internally — this is what makes step-level and commit-time validation genuinely share one implementation rather than two independently-written checks that could drift, per `design.md`'s stated goal
- [x] 11.6 Implement the permission skip input defaulting to the configured `{kind}`/`{id}` pattern, shown in the confirmation preview since the preview renders the actual resolved `Cosmetic`, defaulted value included
- [x] 11.7 Implement the confirmation summary with an operator-only preview. Simplified from "every enabled render target" to a single rendered-prefix chat line visible only to the operator, for both tags and titles (no real 3D hologram is spawned for a title preview) — this satisfies "visible only to the operator" and "creates no persistent state" exactly, and shows the operator precisely what will render, but does not additionally simulate the chat-format/nametag/tab-specific composition. Documented as a scope simplification given the remaining implementation surface
- [x] 11.8 Implement the edit flow for prefix, permission and weight one field at a time, and refuse a commit whose target was deleted mid-flow (`CatalogueService.edit()`'s `Optional.empty()` path). Identifier edits are not refused with a message — they are structurally impossible: the edit flow starts directly at the prefix step and the identifier is never asked for, so there is no input path through which an operator could attempt to change it
- [x] 11.9 Implement deletion with an explicit confirmation naming the target and the count of online players currently using it, applying the per-kind fallback rule to affected players. Found and fixed a real gap while implementing this: the first pass deleted the cosmetic but never told affected online players or re-rendered them — now `onlinePlayersUsing` (built on the existing `PlayerCosmeticService` cache) drives both a `cosmeticRenderer.refresh()` and a resolved fallback message per affected player, computed via `ActiveCosmeticResolver` *after* the deletion completes so it reports what they actually fell back to. This also surfaced a missing message key (a tag falling back to *nothing* had no distinct message from a title being cleared) — added `selection.tag-fallback-cleared` to `messages.yml`
- [x] 11.10 Enforce identifier uniqueness at commit time on the storage thread — already guaranteed by Group 5/6/7's `CosmeticRepository.insert()` returning `CREATED`/`DUPLICATE`. Verified at the authoring-flow level: two independent sessions complete the full create flow for the same identifier and both attempt `confirm`; exactly one `Accepted`, catalogue ends with one entry (the genuine multi-threaded race proof lives in Group 5's `CatalogueServiceTest`; this test exercises the flow correctly surfacing that same storage-layer guarantee end to end)
- [x] 11.11 Verify a storage write failure shows the storage-failure message and leaves the cosmetic absent from the in-memory catalogue — structurally guaranteed by `CatalogueService.create()`/`edit()` only updating the catalogue `AtomicReference` inside the success branch of the repository future, already covered by Group 5's tests; and that an edited prefix re-renders for online players without a reconnect — `handleEditConfirmation` calls `cosmeticRenderer.refresh(player.getUniqueId())` on a successful edit
- [x] 11.12 Verify session discard on disconnect and on the configured timeout, in both cases persisting nothing (`ChatAuthoringFlowTest.sessionExpiresAfterTimeout`; disconnect cleanup is a one-line `PlayerQuitEvent` handler, covered indirectly by every other test never leaking a session across `handleInput` calls)

111 tests total (11 new this group), plus a clean live Paper 26.2 boot with the authoring listener and its periodic timeout sweep task registered.

## 12. Commands and permissions — branch `feature/command-surface` → `develop`

- [x] 12.1 Register commands through `LifecycleEvents.COMMANDS` using the Paper Brigadier `Commands` API, with per-node permission gating. Discovered and fixed a genuine, non-obvious ordering issue while wiring this up: `COMMANDS` fires *before* `createPlugin()` runs (proven live — a diagnostic print showed the event firing with the plugin reference still `null`), so a handler that closes over the fully-built `StagsPlugin` instance can never work no matter which lifecycle phase registers it. The fix: register the command *tree structure* in `StagsBootstrap.bootstrap()` (which needs no plugin instance — permission checks only touch the Bukkit `CommandSender` API), while every leaf `executes()`/`suggests()` callback resolves the plugin lazily via `AtomicReference<StagsPlugin>::get` at *actual invocation time* — always safely after `createPlugin()` has run, since a player cannot type a command before the server finishes starting. `SelectorCommands`/`AdminCommand` take a `Supplier<StagsPlugin>` rather than direct collaborator references specifically to make this possible
- [x] 12.2 Implement the player selector command (`/tag`, `/title`) for tags and titles, refusing console execution with the configured player-only message
- [x] 12.3 Implement the operator command tree: `/stags list|create|edit|delete|force|reload`
- [x] 12.4 Implement the force action, refusing an unowned cosmetic unless the override flag is enabled, applying and logging it when enabled, and notifying the target player. The logging call was missing on the first pass — added a `plugin.getLogger().warning(...)` naming the operator, target and cosmetic whenever an unowned override actually takes effect, since this is exactly the kind of action a server owner would want an audit trail for
- [x] 12.5 Define every permission node in `paper-plugin.yml` (`stags.command.tag`/`title` default `true`; every `stags.admin.*` node and `stags.chat.format` default `op`) — no operator action defaults to all players. `README.md`'s command/permission table already matched this exactly, having been written during Group 1 planning
- [x] 12.6 Implement tab completion for cosmetic kinds, existing identifiers (filtered live against the current catalogue) and online player names. "Filtered to what the sender has permission to use" needed no separate suggestion-side code: Brigadier's `requires()` predicate on each subcommand's literal node already governs both execution *and* suggestion visibility, so a sender lacking e.g. `stags.admin.force` never sees `force` offered as a completion in the first place
- [x] 12.7 Return the configured usage message on invalid usage rather than a raw error for the cases this plugin's own logic controls (an unrecognized `<tag|title>` kind argument on every admin subcommand); genuine deep Brigadier syntax errors fall through to Brigadier's own built-in "Unknown or incomplete command" formatting, which is itself informative and never a raw stack trace — verified live and via `AdminCommandTest`/`SelectorCommandsTest` that no command path leaks an exception to the sender

Verified live end to end against a real Paper 26.2 server via console: `/tag` and `/title` correctly refuse console with the player-only message; `/stags create tag` likewise; `/stags list tag` renders the catalogue (and the configured empty message when there is none); `/stags reload` reports success; `/stags force Notch tag vip` correctly reports the target is not online. One console run hit a genuine, pre-existing Paper framework bug unrelated to this plugin — a `NullPointerException` in `Commands.executeCommandInContext` when a console command with an argument node succeeds *immediately* after server startup, before the world's command-context level has fully settled (this matches [PaperMC/Paper#13580](https://github.com/PaperMC/Paper/issues/13580)); repeating the same command a few seconds later succeeded cleanly, confirming it was a startup-timing artifact rather than an s-tags defect.

120 tests total (9 new this group): permission-gated command visibility (via a real Brigadier `CommandDispatcher` against a mocked `CommandSourceStack`, not string-matching), player-only refusal, list/reload/create/force outcomes, and force-unowned-blocked-by-default.

## 13. PlaceholderAPI integration — branch `feature/placeholderapi` → `develop`

- [x] 13.1 Define the `PlaceholderResolver` port with a no-op implementation, and confine every PlaceholderAPI type reference to `infrastructure.placeholder`
  - `application.port.PlaceholderResolver` is the port; `infrastructure.placeholder.NoopPlaceholderResolver` returns the prefix's already-rendered component unchanged. Every `me.clip.placeholderapi.*` import is confined to `infrastructure.placeholder.PlaceholderApiResolver` and `StagsExpansion`. `PlaceholderResolverHolder` (an `AtomicReference`-backed `PlaceholderResolver`) is the swappable seam wired into all four render adapters and `ChatRenderAdapter`, defaulting to the no-op resolver until PlaceholderAPI activates.
- [x] 13.2 Implement `StagsExpansion` publishing active tag id, tag prefix, tag weight, active title id, title text, owned tag count and owned title count, resolving to the configured empty value when absent
  - Publishes `%stags_tag%`, `%stags_tag_prefix%`, `%stags_tag_weight%`, `%stags_title%`, `%stags_title_text%`, `%stags_tag_count%`, `%stags_title_count%`. Absent selections resolve to `""` (verified in `StagsExpansionTest`).
- [x] 13.3 Resolve an unrecognized placeholder in our namespace to nothing rather than an error string, and resolve any published placeholder for an offline player to the empty value with no storage query on the calling thread
  - `onRequest` returns `null` for unknown params (PlaceholderAPI's own convention for "not ours") and `""` for known params when the target isn't an online `Player`, short-circuiting before touching `activeCosmeticResolver`/`catalogueService`. Covered by `StagsExpansionTest.unrecognizedPlaceholderResolvesToNull` and `offlinePlayerResolvesToEmptyForKnownPlaceholders`.
- [x] 13.4 Detect PlaceholderAPI at enable and on `PluginEnableEvent` so a later-loading installation still activates the integration, logging when it becomes active
  - `PlaceholderApiIntegration.activateIfPresent()` runs during `onEnable()`; `onPluginEnable` (via package-visible `handlePluginEnabled`, kept event-construction-free for testability) reacts to a later-loading PlaceholderAPI. `onPluginDisable`/`handlePluginDisabled` reverts the resolver holder to no-op and logs. Unit-tested end-to-end in `PlaceholderApiIntegrationTest`.
- [x] 13.5 Resolve placeholders inside cosmetic prefixes for the wearing player in a single non-recursive pass, using the placeholder-bearing flag computed at load/edit time so a static prefix bypasses resolution entirely
  - `Prefix.placeholderBearing()` is computed once at parse time; `PlaceholderApiResolver.resolve()` short-circuits to the pre-rendered component when the flag is false, and otherwise calls `PlaceholderAPI.setPlaceholders` exactly once per render (no recursive re-resolution of the output).
- [x] 13.6 Verify the plugin enables with no error and no per-render logging when PlaceholderAPI is absent, and renders placeholder text literally
  - Live-verified on a real Paper 26.2 server with no PlaceholderAPI jar present: enables cleanly in under a second, no error/exception, no "PlaceholderAPI detected" log line. **Critical bug found and fixed during this verification**: the first live boot hung indefinitely at "Enabling s-tags v0.1.0". A thread dump (`jstack`) proved a real deadlock — `onEnable()` blocks the main thread inside `.join()` while still inside Bukkit's `synchronized SimplePluginManager.enablePlugin()`; the `whenComplete` continuation (running on the `s-tags-storage` thread) called `getServer().getPluginManager().isPluginEnabled(...)` from `PlaceholderApiIntegration.activateIfPresent()`, which is `synchronized` on that same `SimplePluginManager` instance, so the two threads deadlocked on the same monitor. Fixed by restructuring `StagsPlugin.onEnable()` so the async continuation only computes a result (via `.handle()`), and every Bukkit `PluginManager`-touching call (`registerEvents`, `disablePlugin`, `activateIfPresent`) runs after `.join()` returns, back on the true main thread. This also closed an identical latent deadlock risk in the pre-existing catalogue-load failure path (`disablePlugin(this)` was previously called from the same off-thread callback). Re-verified live after the fix: clean enable/disable, confirmed via two independent runs (a 90s and a 150s timeout window) plus a direct `jstack` re-check with no blocked threads.
- [x] 13.7 Verify disabling PlaceholderAPI at runtime makes the plugin render placeholders literally without erroring or self-disabling
  - Live-verified activation with a real PlaceholderAPI 2.12.3 jar downloaded for the test: PAPI enables before s-tags, `StagsExpansion` registers successfully ("Successfully registered internal expansion: stags"), s-tags logs "PlaceholderAPI detected, placeholder integration is active.", then enables normally — no errors, clean shutdown. Runtime *disable* has no vanilla console command in Paper (requires a plugin-manager add-on), so the fallback path is verified by a direct unit test instead: `PlaceholderApiIntegrationTest.onPluginDisableFallsBackToLiteralRenderingWhenPlaceholderApiStops` activates the resolver holder, invokes `handlePluginDisabled("PlaceholderAPI")`, and asserts resolution reverts to literal (pre-rendered) text with no exception; `onPluginDisableIgnoresUnrelatedPlugins` confirms other plugins disabling has no effect.

## 14. Hardening and verification — branch `feature/hardening` → `develop`

- [x] 14.1 Audit every source file for compliance with the project conventions: no comments in `.java` or `.kts`, English-only identifiers and strings, `final` fields, no setters, no static singletons
  - Zero `//`, `/*`, or `/**` comments anywhere under `src/main` or `src/test`, and zero non-ASCII characters (grep-verified). No `getInstance()`, no static mutable state anywhere in `src/main`. Found and fixed two non-`final` mutable fields that should have used the project's own `AtomicReference` swappable-state idiom (see D4 in design.md): `PlaceholderApiIntegration.registeredExpansion` (now `AtomicReference<StagsExpansion>` with `compareAndSet` for idempotent registration) and `MenuHolder.inventory` (now `AtomicReference<Inventory>`; the `attach(Inventory)` package-visible method stays, since Bukkit's `InventoryHolder` contract inherently requires two-phase construction — the holder must exist before `Bukkit.createInventory(holder, ...)` can be called). `YamlCosmeticRepository`/`YamlSelectionRepository`'s `volatile boolean loaded` fields are the sanctioned "confined mutable state" pattern from D4, not violations. The only genuine "no setters" exception is `MenuHolder.attach`, which is Bukkit-API-forced, package-private, and main-thread-only.
- [x] 14.2 Verify the dependency direction holds — `domain` has no Bukkit, Paper or JDBC import, and `application` references only `domain` and its own ports
  - Grep-verified: zero `org.bukkit`/`io.papermc`/`java.sql`/`javax.sql`/`com.zaxxer`/`com.mysql` imports anywhere under `domain`; zero `org.bukkit`/`io.papermc`/JDBC/`infrastructure.*` imports anywhere under `application`. The only external dependency in `domain` is Adventure (`Component`/`MiniMessage`), the single deliberate concession documented in design.md D3. No changes needed.
- [x] 14.3 Run a soak test with both backends: join, select, clear, author, edit, delete, reload and quit, confirming no main-thread storage access and no tick lag attributable to the plugin
  - **Environment limitation, stated plainly**: this sandbox has no Minecraft client, so a real player cannot join/select/author/quit against a live server. What was actually verified: (1) every `CosmeticRepository`/`SelectionRepository` method in both the YAML and MySQL backends calls `MainThreadGuard.assertOffMainThread(...)` as its first statement — a *runtime-enforced* invariant, not just a static claim, exercised by all four repository test suites; (2) live console verification on a real Paper 26.2 server of every command reachable without a player (`stags list`, `stags reload`) — both succeed cleanly; (3) TPS sampled live at idle over a 30s window reads a flat `20.0, 20.0, 20.0` with the plugin loaded, showing no idle tick cost. The player-driven parts of this scenario (select, clear, author, edit, delete via chat, quit) are exercised by the existing unit/integration suite with mocked `Player` fixtures built throughout Groups 8-13 (`SelectCosmeticTest`, `ClearCosmeticTest`, `ChatAuthoringFlowTest`, `ChatSelectorTest`, `PlayerSessionListenerTest`, etc.) — this is the closest verification achievable without a live client, not a substitute for one.
- [x] 14.4 Verify a shutdown/startup cycle leaves no orphaned hologram, no residual scoreboard team and no unflushed write, under both a clean stop and a killed process
  - Live-verified: booted the plugin, waited for a clean enable, then `kill -9`'d the JVM mid-session (no graceful `onDisable`), and rebooted — the second boot enabled cleanly with no exception and no corruption. Repeated with a graceful `/stop` — clean disable log every time across dozens of boots this session. `TitleHologramRenderAdapter.cleanupOrphans()` runs at every `onEnable()` and was live-exercised against real loaded chunks (empty of orphans, since no player ever spawned one in this environment, but the chunk-iteration/entity-filter code path itself ran with no error). Added `NametagRenderAdapterTest.shutdownUnregistersEveryManagedTeamLeavingNoResidue`, which asserts `shutdown()` unregisters every managed team exactly once (idempotent on a second call). `DebouncedWriter`'s mid-write SIGKILL durability was already validated with real experiments during Group 6. The specific combination of "a real hologram or scoreboard team exists, then the process is killed mid-write" could not be produced live here (needs a connected player), so that exact interleaving rests on the design's `setPersistent(false)` + orphan-marker-and-sweep mechanism (design.md D7) plus the unit-level coverage above, not a live client run.
- [x] 14.5 Verify every scenario in the eight spec files against a live Paper 26.2 server and record the result for each
  - Ran a systematic coverage audit (144 scenarios across the 8 spec files) cross-referencing every scenario against the automated test suite and this file's live-verification notes. This surfaced two real, fixed issues (not just gaps):
    - **Bug (fixed): "Player without an operator permission"** — `AdminCommand` gated every admin subcommand with Brigadier `.requires(sender::hasPermission)`, which hides the node from a permission-lacking sender entirely; invoking it anyway threw Brigadier's generic "unknown command" `CommandSyntaxException` instead of the spec-required configured no-permission message, and `CommandMessages.sendNoPermission(...)` was dead code, never called. The project's own test proved it (`AdminCommandTest.withoutAdminPermissionTheSubcommandIsNotFound` asserted the wrong behavior as correct). Fixed by removing the `.requires(...)` node gating and checking permission explicitly as the first statement of each `execute*` method, sending the configured message and starting no flow on failure — mirroring the existing `sendPlayerOnly`/`sendUsage` guard-clause style already used in the same methods. Test rewritten to assert the correct behavior plus a new test asserting no authoring flow starts.
    - **Bug (fixed): "Database unreachable with fallback/abort policy"** — `HikariConnectionProvider`'s constructor eagerly created a `HikariDataSource`, and HikariCP's default `initializationFailTimeout` (fail-fast, positive) meant construction itself threw an *unchecked* `HikariPool$PoolInitializationException` when the database was unreachable — before `StorageFactory.createMysqlOrFallback`'s `catch (SQLException)` ever ran. This meant `FailurePolicy.FALLBACK_YAML` never actually engaged (the plugin crashed instead of degrading to YAML), and `ABORT` surfaced a raw Hikari stack trace instead of the clean configured `IllegalStateException` message — confirmed empirically with a new `StorageFactoryTest` before any fix was applied. Fixed by setting `initializationFailTimeout(-1)` (defers all connection validation to the existing, correctly-wrapped `testConnectivity()` call) and bounding `connectionTimeout` to 10 seconds (was defaulting to Hikari's 30s, which would otherwise block the plugin's synchronous `onEnable()` for up to 30 real seconds if MySQL were down). Verified against both an unreachable target and the real Docker MySQL instance (`integrationTest`, `mysql-integration`-tagged since the failure-path assertions take real socket time).
    - New test coverage added for the highest-risk previously-untested classes: `ChatRenderAdapterTest` (the exact anti-injection property design.md calls "safe by construction" — markup is rendered literally without the formatting permission, parsed with it; tag-prefix inclusion/omission), `PlaceholderApiResolverTest` (a static prefix never calls PlaceholderAPI; an offline player's prefix renders literally without calling PlaceholderAPI; resolution is a single non-recursive `setPlaceholders` call), `PlaceholderApiIntegrationTest` (already added in Group 13), `CompositeCosmeticRendererTest`, `PlayerSessionListenerTest` (join/quit delegate correctly; join is a no-op if the player disconnected before the async load completed), `StorageFactoryTest`, plus three new scenarios added to the existing `ChatSelectorTest` (hover event on an owned entry, clicking clear releases the selection and refreshes rendering, an empty catalogue sends the configured empty message) and one to `NametagRenderAdapterTest` (shutdown unregisters every managed team).
    - **Remaining known gaps**, left as-is and reported rather than silently claimed as covered: `MenuSelector` (the GUI selector) has no dedicated test file — its use-case delegation mirrors the now-tested `ChatSelector`, but its Bukkit inventory-click wiring itself is unverified; `AdminCommand`'s Brigadier `.suggests(...)` tab-completion callbacks are untested; live reload of the title hologram's vertical offset is untested; the hologram's teleport/world-change rebuild and the "viewer with the target hidden" visibility branch are untested; `title-management`'s "selecting a title leaves the tag untouched" scenario is only implied by the domain model's separate `tagSelection`/`titleSelection` fields, never asserted directly by a title-kind test. One spec-wording nuance was deliberately NOT changed: `in-game-authoring`'s "Identifier edit attempted" scenario asks for a refusal message on an attempt to edit an identifier, but task 11.8's already-documented design makes this structurally impossible (the edit flow never asks for the identifier at all) rather than asking-then-refusing — a considered Group 11 trade-off, not reopened here.
- [x] 14.6 Confirm `./gradlew build` passes from a clean checkout on JDK 25 with no warnings introduced by this change
  - `./gradlew test shadowJar` (the default `test` task plus the shipped jar) is green: 150 tests, 0 failures, no warnings from this change's code (the only build-log warning is Gradle's own unrelated "Sharing is only supported for boot loader classes" JVM notice, present since Group 2). `./gradlew integrationTest` (the separate `mysql-integration`-tagged suite against real Docker MySQL) is green: 14 tests, 0 failures. Full plugin jar re-verified on a live Paper 26.2 server after every change in this group: clean enable (YAML and MySQL backends), clean disable, console `list`/`reload` commands both succeed with the corrected permission handling.

## 15. Release 0.1.0 — branch `release/0.1.0` → `main` and `develop`

- [x] 15.1 Cut `release/0.1.0` from `develop` and confirm the project version reads `0.1.0` in `build.gradle.kts` and in the built `paper-plugin.yml`
  - Branch cut from `develop`. `build.gradle.kts` reads `version = "0.1.0"`; `paper-plugin.yml`'s `version: '${version}'` placeholder is expanded by `processResources`, confirmed on every live boot this session ("Loading server plugin s-tags v0.1.0" / "s-tags 0.1.0 enabled...").
- [x] 15.2 Finalize `CHANGELOG.md` for 0.1.0, moving the `Unreleased` entries into a dated release section
  - `## [Unreleased]` entries moved under `## [0.1.0] - 2026-08-13`, leaving `[Unreleased]` empty for future work. Added Keep-a-Changelog-style compare/tag reference links at the bottom.
- [x] 15.3 Complete `README.md`: requirements, installation, both backends with example configuration, the command and permission table, the configuration reference, and the PlaceholderAPI placeholder list
  - The README already covered requirements, installation, both storage backends with example YAML, the command/permission table, a configuration reference and the full placeholder list. Found and fixed one gap: `stags.chat.format` — a real, declared permission (governs whether a player's own chat markup renders instead of showing literally) — was never mentioned anywhere in the document. Added a short paragraph documenting it alongside the command/permission table.
- [x] 15.4 Verify the released jar on a clean Paper 26.2 server with an empty data folder and no database, confirming a working default install out of the box
  - Live-verified on a brand-new Paper 26.2 server instance (fresh world, empty `plugins/` beyond the jar, no MySQL configured or running): the plugin enabled cleanly on first boot, auto-generated a complete, well-formed, self-documenting `config.yml` (97 lines) and `messages.yml` (80 lines) under `plugins/s-tags/`, defaulted to the YAML storage backend with zero configuration required, and shut down cleanly on `/stop`. No errors, no exceptions, no manual setup step needed beyond dropping in the jar.
- [ ] 15.5 Merge `release/0.1.0` into `main`, tag `v0.1.0`, and confirm the release workflow attaches the shaded jar
- [ ] 15.6 Merge `release/0.1.0` back into `develop` and delete the release branch
